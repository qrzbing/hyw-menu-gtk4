# Hovering Yonder Wind

A configurable desktop shell for Wayland compositors. Built in Rust with GTK4.

## Usage

Directly by Nix.

Install `gst-plugin-gtk4` on your system:

```shell
# Arch Linux
sudo pacman -S gst-plugin-gtk4
```
